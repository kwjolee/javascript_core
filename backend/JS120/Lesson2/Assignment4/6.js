/*
one can set the prototype object of an object as `null`, which will effectively clear it of all prototypes
if the object's prototype object is `null`, that is a fairly good indication that it does not have prototypes
*/