/*
the variable `context` is assigned to the return value of invoking the function `func`
`func` returns the value of its `this` object.
since `func` is an ordinary function, its `this` object is the global object
so line 7 outputs the global object.
*/